# Tiered coverage policy (intentproof-sdk-go)

| Tier | Minimum | Scope |
|------|---------|--------|
| **Total** | 90% | `intentproof/` package (excludes vendored `jcs.go`) |
| **Critical** | 95% | `intentproof/` package excluding vendored `jcs.go` |

Configuration: `scripts/coverage-tiers.conf`.
